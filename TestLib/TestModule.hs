--A test module that will be in the project while i am developing the cmake macros for haskell projects

module TestLib.TestModule (testFunction) where

testFunction :: Int -> Int
testFunction x = 2 * x
